name: Mobile Waldo Subscribers
on: 
  push:
  workflow_dispatch:
  repository_dispatch:
    types: [create_sub]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Test Greeting
        run: echo "hello nena"